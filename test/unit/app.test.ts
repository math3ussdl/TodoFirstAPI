import { expect } from 'chai';

describe('App test suite', function () {
  it('should returns the sum between 5 and 5 (10)', function () {
    const sum = 5 + 5;

    expect(sum).to.equal(10);
  });
});
